# Featured Slider

A small demo app implementing a premium horizontal project slider using React + Tailwind CSS + Framer Motion.

Quick start:

1. cd featured-slider
2. npm install
3. npm run dev

This project is intentionally minimal; integrate into your existing site or adapt the component at `src/components/FeaturedSlider.jsx`.
